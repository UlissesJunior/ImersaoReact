import React from 'react';

const message = (props) => {
    return(
        <div className='container'>
            {/* {props.message.map(() =>{
                    return(
                      <li key={message.id}><h1>{message.from}: {mensagematual.text}</h1></li>
                    )
            })} */}
        </div>
        
    )
}

export default message;